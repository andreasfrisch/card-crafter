'use strict';

describe('cardCrafter', function() {

  beforeEach(module('cardCrafter'));

  describe('cardCrafter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cardCrafterController = $controller('CardCrafterController');
      expect(cardCrafterController).toBeDefined();
    }));

  });
});